// 统一中转工具
import { request } from "@/utils/request"
import { addLocalToken, getLocalToken, removeLocalToken } from "@/utils/token"

export {
    request,
    addLocalToken,
    getLocalToken,
    removeLocalToken
}

